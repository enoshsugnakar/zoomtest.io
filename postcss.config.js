module.exports = {
  plugins: {
    // This tells PostCSS to load the `@tailwindcss/postcss` package
    // under the `tailwindcss` key, with an empty options object:
    tailwindcss: {},
    // And load autoprefixer as usual:
    autoprefixer: {},
  },
};
